[project]
name = "reins"
version = "2.0.0"
description = "Deterministic contract pipeline, runtime-native: schemas, validator, frontier, telemetry — zero dependencies, invoked by path, never installed"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
