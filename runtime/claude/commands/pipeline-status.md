---
description: Show pipeline status for a task. Usage - /pipeline-status <task-id>
---
Run `python3 ~/.claude/pipeline/core/pipeline_cli.py status $ARGUMENTS --json` and explain the status, next contract,
and what (if anything) the human must do, per docs/state-machine.md.
