---
description: Validate a task's artifact chain. Usage - /validate <task-id>
---
Run `python3 ~/.claude/pipeline/core/pipeline_cli.py validate $ARGUMENTS`, then explain each violation and which
contract re-run or edit repairs it. Do not fix artifacts without being asked.
